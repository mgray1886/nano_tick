# Local-test broker config; mirrors the 4B setup from the README.
# (In the container, binding all interfaces is fine - the compose network
# is isolated. On the 4B, bind to 192.168.100.2 instead.)
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
max_queued_messages 100000
